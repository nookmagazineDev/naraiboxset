import React from 'react';

// แสดงคำขออนุมัติ QR ให้แอดมิน/แคชเชียร์กดยืนยัน (ปิดตัวฟังคำขออนุมัติข้ามเครื่องแล้วเนื่องจากยกเลิกระบบยืนยัน QR)
const PaymentApprovalListener = () => {
  return null;
};

export default PaymentApprovalListener;
